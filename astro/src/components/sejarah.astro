---

---

<html lang="id">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>IsFor Pusat Riset Informatika</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Inter:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
      @keyframes float {
        0%,
        100% {
          transform: translateY(0);
        }
        50% {
          transform: translateY(-20px);
        }
      }

      @keyframes pulse {
        0%,
        100% {
          opacity: 0.5;
        }
        50% {
          opacity: 0.8;
        }
      }

      @keyframes slideInLeft {
        from {
          opacity: 0;
          transform: translateX(-50px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes slideInRight {
        from {
          opacity: 0;
          transform: translateX(50px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      .animate-float {
        animation: float 6s ease-in-out infinite;
      }

      .animate-slide-left {
        animation: slideInLeft 0.8s ease-out forwards;
      }

      .animate-slide-right {
        animation: slideInRight 0.8s ease-out forwards;
      }

      .swiss-grid {
        display: grid;
        grid-template-columns: repeat(12, 1fr);
        gap: 1rem;
      }

      @media (max-width: 768px) {
        .swiss-grid {
          gap: 2rem;
        }
      }

      .typography-display {
        font-family: "Space Grotesk", sans-serif;
      }

      .typography-body {
        font-family: "Inter", sans-serif;
      }

      .btn-primary {
        transition: all 0.3s ease;
        transform-origin: center;
      }

      .btn-primary:hover {
        transform: translateY(-2px) scale(1.02);
      }

      .illustration-wrapper {
        position: relative;
      }

      .illustration-wrapper::before {
        content: "";
        position: absolute;
        top: -10%;
        right: -10%;
        width: 70%;
        height: 70%;
        background: #3b82f6;
        opacity: 0.1;
        border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
        z-index: -1;
        animation: pulse 10s ease-in-out infinite;
      }

      @keyframes blob {
        0%,
        100% {
          transform: translate(0, 0) scale(1);
        }
        25% {
          transform: translate(20px, -20px) scale(1.1);
        }
        50% {
          transform: translate(-20px, 20px) scale(0.9);
        }
        75% {
          transform: translate(20px, 20px) scale(1.1);
        }
      }

      .animate-blob {
        animation: blob 7s infinite;
      }

      .animation-delay-2000 {
        animation-delay: 2s;
      }
    </style>
    <!-- SEJARAH -->
  </head><section id="sejarah" class="py-16 md:py-24 bg-white">
    <div class="container mx-auto px-4 md:px-6 lg:px-12 ">
      <!-- Header Section -->
      <div class="text-center mb-12 md:mb-16 animate-fade-in-up animate-float">
        <span
          class="inline-block px-3 md:px-4 py-1 bg-purple-100 text-purple-700 rounded-full text-xs md:text-sm font-medium mb-4"
          >Didirikan 2021</span
        >
        <h1 class="text-3xl md:text-4xl font-bold text-purple-700 mb-4">
          Sejarah Kami
        </h1>
        <div class="w-12 md:w-16 h-1 bg-purple-600 mx-auto"></div>
      </div>

      <!-- Content Grid -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 md:gap-12">
        <!-- Left Column - Image -->
        <div
          class="overflow-hidden transform hover:scale-105 transition-transform duration-500 animate-fade-in"
        >
          <img
            src="/images/Logo1.png"
            alt="IsFor Research Center"
            class="w-48 md:w-64 lg:w-80 mx-auto object-contain"
          />
        </div>
        <!-- Right Column - Text Content -->
        <div class="space-y-4 md:space-y-6 animate-fade-in-up">
          <div class="space-y-6 md:space-y-8">
            <div class="pl-4 md:pl-8 border-l-4 border-purple-200">
              <h3 class="font-bold text-lg md:text-xl text-purple-700">
                Tentang IsFor
              </h3>
              <p class="text-sm md:text-base text-gray-600">
                Pusat Riset IsFoR (Internet of Things for Human Life) didirikan
                pada Tahun 2021 untuk mewadahi berbagai riset dosen dan
                mahasiswa tentang IoT berbasis Teknologi IoT.
              </p>
            </div>

            <div class="pl-4 md:pl-8 border-l-4 border-purple-200">
              <h3 class="font-bold text-lg md:text-xl text-purple-700">
                Inovasi Kami
              </h3>
              <p class="text-sm md:text-base text-gray-600">
                Produk inovasi berbasis IoT meliputi IoT Edu Board, Urban
                Farming, Budidaya Lele IoT, Video Live Streaming Lalu Lintas,
                dan sistem pendukung Deep Learning.
              </p>
            </div>

            <div class="pl-4 md:pl-8 border-l-4 border-purple-200">
              <h3 class="font-bold text-lg md:text-xl text-purple-700">
                Kolaborasi
              </h3>
              <p class="text-sm md:text-base text-gray-600">
                IsFor berkolaborasi dengan Miota, PT. Symposs, dan Unimas dalam
                pengembangan IoT.
              </p>
            </div>
          </div>

          <!-- Stats -->
          <div class="grid grid-cols-3 gap-2 md:gap-4 pt-6 md:pt-8">
            <div class="text-center animate-fade-in-up">
              <div class="text-2xl md:text-3xl font-bold text-purple-700">
                3+
              </div>
              <div class="text-xs md:text-sm text-gray-600">
                Partner Institusi
              </div>
            </div>
            <div class="text-center animate-fade-in-up">
              <div class="text-2xl md:text-3xl font-bold text-purple-700">
                5+
              </div>
              <div class="text-xs md:text-sm text-gray-600">Produk Inovasi</div>
            </div>
            <div class="text-center animate-fade-in-up">
              <div class="text-2xl md:text-3xl font-bold text-purple-700">
                2021
              </div>
              <div class="text-xs md:text-sm text-gray-600">Tahun Berdiri</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</html>
